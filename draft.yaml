models:
  Realisation:
    title: text
    thumbnail: text
    relationships:
      belongsToMany: RealisationCategory

  RealisationCategory:
    title: text unique
    slug: text unique
    relationships:
      belongsToMany: Realisation